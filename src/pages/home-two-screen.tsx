import { GlobeLoader } from "@/components/shared-assets/globe-loader";

/** /home2 — alternative landing using the GlobeLoader (full-screen overlay). */
export const HomeTwoScreen = () => {
    return <GlobeLoader />;
};
